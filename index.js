const request = require('request');
const cheerio = require('cheerio');
const fs = require('fs');

const Movie = function () {
  this.name = '';
  this.score = 0;
  this.quote = '';
  this.ranking = 0;
  this.coverUrl = '';
};

const main = function () {

};

main();